# C-timer
#### a pomodoro timer in C

The user can define the variables through flags; notifications are simply printed in the terminal.

Sound files were made using an online synth and Audacity. They are located in ~/.local/share/ctimer
